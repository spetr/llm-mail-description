# Hugging Face token for gated models (e.g., Llama)
HF_TOKEN=your_huggingface_token_here

# Environment
ENVIRONMENT=development

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Config path (default: /config in Docker, ./config locally)
CONFIG_PATH=./config
